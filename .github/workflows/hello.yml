name: Hello GitHub Actions

on: push   # triggers when somebody pushes code

jobs:
  demo_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions"
